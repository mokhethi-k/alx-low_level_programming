#include "main.h"
/**
 * mul - check for multiples
 * @a: first number to be checked
 * @b: Second number to be checked
 * Return: returns the multiples of the two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
